# Retain as Curated Shared Memory

Use when an inbox candidate is a short reusable fact, convention, pitfall, or durable project state.

Destination:

- `knowledge/shared-memory/workspace/<name>.md`
- `knowledge/shared-memory/module/<module>/<name>.md`
- `knowledge/shared-memory/capability/<capability>/<name>.md`

Rules:

- Convert inbox metadata to curated shared-memory frontmatter.
- Preserve `source: agent:<platform>-<model>` provenance.
- Keep evidence.
- Update `workspace/MEMORY.md` and workspace guide file only for workspace scope.
