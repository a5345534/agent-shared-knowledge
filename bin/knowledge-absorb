#!/usr/bin/env bash
# knowledge-absorb — Inbox absorption CLI
# Installed as part of the agent-shared-knowledge Pi package.
set -euo pipefail
DIR="$(cd "$(dirname "$(readlink -f "$0")")/.." && pwd)"
exec python3 "$DIR/scripts/knowledge_absorb.py" "$@"
